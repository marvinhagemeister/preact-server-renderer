{
  "name": "preact-server-renderer",
  "version": "0.2.1",
  "description": "Server-side renderer for preact",
  "main": "dist/index.js",
  "author": "Marvin Hagemeister <marvin@marvinhagemeister.de>",
  "license": "MIT",
  "typings": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git@github.com:marvinhagemeister/preact-server-renderer.git"
  },
  "scripts": {
    "bench": "ts-node benchmark/index.ts",
    "cleanup": "rimraf dist/",
    "lint": "tslint 'src/**/*.ts'",
    "watch": "tsc -w",
    "build": "npm run cleanup && tsc",
    "test": "mocha --watch-extensions tsx,ts  --compilers ts:ts-node/register 'src/**/__tests__/*.spec.*'",
    "test-watch": "npm t -- -w -R min",
    "coverage": "nyc npm t",
    "format": "prettier --trailing-comma=all --write 'src/**/*.{ts,tsx}'",
    "prepublish": "npm run lint && npm t && npm run build",
    "precommit": "lint-staged"
  },
  "peerDependencies": {
    "preact": "*"
  },
  "dependencies": {
    "@types/node": "^8.0.26",
    "vdom-utils": "^0.3.1"
  },
  "devDependencies": {
    "@types/benchmark": "^1.0.30",
    "@types/chai": "^4.0.4",
    "@types/chalk": "^0.4.31",
    "@types/mocha": "^2.2.42",
    "@types/sinon": "^2.3.3",
    "benchmark": "^2.1.4",
    "chai": "^4.1.2",
    "chalk": "^2.1.0",
    "husky": "^0.14.3",
    "lint-staged": "^4.0.4",
    "mocha": "^3.5.0",
    "nyc": "^11.1.0",
    "preact": "^8.2.5",
    "preact-render-to-string": "^3.6.3",
    "prettier": "^1.6.1",
    "rimraf": "^2.6.1",
    "sinon": "^3.2.1",
    "ts-node": "^3.3.0",
    "tslint": "^5.7.0",
    "tslint-config-sevenval": "^0.6.3",
    "typescript": "^2.5.2"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --trailing-comma=all --write",
      "npm run lint",
      "git add"
    ]
  },
  "nyc": {
    "include": [
      "src/*.ts",
      "src/**/*.ts"
    ],
    "exclude": [
      "typings",
      "node_modules/",
      "**/__tests__/**",
      "__tests__/**",
      "**/*.d.ts"
    ],
    "extension": [
      ".ts"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [
      "json",
      "html",
      "text",
      "text-summary",
      "lcovonly"
    ],
    "all": true
  }
}
